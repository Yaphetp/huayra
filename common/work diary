#coding=utf-8
"""
1、登录判断（如果未登录，则登录；如果已登录，退出登录再登录）
2、启动后插屏广告判断
3、启动后引导页判断
4、判断元素是否存在（页面是否出现时使用）

1、不同py类测试用例，加入测试套件，执行并显示结果 ，同添加测试用例
问题：日期查询，日期、时间控件无法定位。明天试试坐标定位吧
ok 已解决

2、问题：批量约课，如何判断点击到可约课的时间点（而不是置灰的位置）
更多问题1：至少不允许点击预约当天的课程
解决：已解决。使用自定义函数随机点击，控制点击范围今天之外
问题2：
if 与 assertEqual 不能一起使用？
问题3：
滑动坐标点，如果划不动时异常处理。
向下滑动
向左滑动

工作问题：出现约课时，该时间段已经预约的情况
解决：去首页取消预约，判断有课程就取消，直到取消所有课程。 完整的类，取消单节约课。

三种等待，已解决
约课三节课时
已解决：通过or来解决，那是不可能的。通过in来解决，ok
问题：约课失败冲突，提示：你9点已经预约了该课程。

点击系统键盘确认，查询
已解决：0113工作记录
1、使用keycode，但使用的是回车的keycode，并不是搜索的keycode；
2、使用sendkeys出现输入不完整情况，使用hide_keyboard()方法解决问题; 目前在desired_caps统一设置不展示键盘（resetKeyboard=True）。

0114工作记录
问题：send_keys中文输入,折腾了很久（先clear再send、sent_text\sendText、中文.decode(utf-8)）
解决：然而答案超级简单，就特么粗心了啊。 'unicodeKeyboard': True, 'resetKeyboard': True
教训：要细心，不要着急。1分钟的事，搞了半天时间。


问题：是否成功吊起系统、第三方支付判断
答案：目前测试分享，是可以掉起微信，

问题：
# 男 女 滑动上下选择；
# 出生年月上下滑动选择
解决：滑动解决，判断数据正确性，无过多操作。

# toast判断
解决：使用网友提供的方法即可

正则表达式困难的不是匹配到想要的内容，而是尽可能的不匹配到不想要的内容。
# 正则表达式，findall（p1（p1=re.compile(p(p=r'[pP]ython'))））返回所有的匹配结果
x+ 至少匹配一个x，包括+前面的x
x* 可以匹配0次x或多次，包括*前面的x
. 需要使用转义字符\
re.search(): look for a match
正则表达式，一个表达式代表一个字符
[^0-9] 0~9全部取反
\d = [0-9]
\D = [^0-9]
\w = 任意字母数字下划线

向前查找：目标str后面存在（肯定式向前茶早）\不存在（否定式向前查找）指定的字符序列，进行匹配
(?= xxx)、(?! xxx)
向后查找：目标str前面存在……
(?<= xxx)、(?!= xxx)


回溯引用是正则表达式内的一个“动态”的正则表达式
e.g:
key = r"<h1>hello world</h3>"
p1 = r"<h([1-6])>.*?</h\1>"

re.match()
re.search()
re.findall()
每一个方法都接收一个正则表达式和一个待查找匹配的字符串

"""
# 问题：
# 自动化查询结果（查询、列表显示等） 与数据库查询结果对比结果正确性。
# 提前截图与结果对比，根据对应字段，详情页面选择对应的截图对比。
# 1、选择本地照片时，是否可以通过已有示例照片与本地照片对比，对比一致则上传？？
# 2、相机拍照如何实现？？
# 3、不同的手机，本地照相机、本地图片选择的 控件id都是不一样的。这个上传如何测试？
#     如果业内大部分手机有统一的命名，那么可根据当前所测试的手机使用响应的id进行定位操作。
    # 回答: 不同手机的控件ID不同，所以目前只能在指定手机进行上传照片测试
# python 自动化语音识别
# 2、截图对比文字\
# 截图到指定目录》如何删除已有图片（指定图片，所有图片）？
    # 回答：已解决

异或
汉明距离
？ 图像比对原理

1、图片转换为2进制
2、灰度图、二值图像

汉明距离函数实现。